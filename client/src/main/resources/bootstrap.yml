spring:
  application:
    name: client
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        hostname: consul
        prefer-agent-address: true
        prefer-ip-address: true
    config:
      label: master
      discovery:
        enabled: true
        service-id: config
      profile: pro
      fail-fast: true
#      uri:
#        - http://localhost:9090
#      name: config
#      profile: pro
